import user from "./user";
import thread from "./thread";

export const USER_ACTIONS = user;
export const THREAD_ACTIONS = thread;

// Export All Actions.
export default {
    USER_ACTIONS: user,
    THREAD_ACTIONS: thread,
}